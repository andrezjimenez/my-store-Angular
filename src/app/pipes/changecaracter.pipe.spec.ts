import { ChangecaracterPipe } from './changecaracter.pipe';

describe('ChangecaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangecaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
